# Setup guide

This is etl pipeline to extract financial data from excel file, transform the data into a standardized format, and load it into a PostgreSQL database.

## Prerequisite
Docker installed

## Installation

Start the postgres db in Docker

```bash
docker-compose up db
```

```bash
docker-compose build etl
```

## Usage

```bash
docker-compose run --rm etl python3 etl.py
```
